import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nailsgestion.app',
  appName: 'nailsApp',
  webDir: 'dist/nails-app',
};

export default config;
